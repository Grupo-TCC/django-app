# Railway-optimized requirements for InnovaSus
# Fast installation, PostgreSQL-focused

# Django Core
asgiref==3.8.1
Django==5.2
sqlparse==0.5.3
typing_extensions==4.13.2

# Database (PostgreSQL for Railway)
dj-database-url==2.1.0
psycopg2-binary==2.9.7

# Web server and static files
gunicorn==22.0.0
whitenoise==6.7.0

# Environment management
python-dotenv==1.1.0
django-environ==0.11.2

# Security and CORS
django-cors-headers==4.4.0

# File handling (for media uploads)
Pillow==10.0.0

# Only install AWS packages when needed (commented out for Railway)
# boto3==1.35.48
# botocore==1.35.48
# mysqlclient==2.2.4
# PyMySQL==1.1.1